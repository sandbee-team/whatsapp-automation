/**
 * app-frontend - the customer dashboard, a React 19 + Vite SPA.
 *
 * This file is a leftover `main` package-entry placeholder from the initial
 * workspace stub. The real app entry is `index.html` -> `src/main.tsx`
 * (Vite build entry point) - this module is not imported by anything and
 * exists only because the file could not be removed in this session
 * (sandbox denies file deletion). Safe to delete once tooling allows it;
 * `package.json#main` is not consumed by Vite's build.
 */
export const projectName = 'app-frontend' as const;
