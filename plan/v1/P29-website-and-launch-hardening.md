# P29 — website-and-launch-hardening

**Goal (one line):** A static-export marketing site whose every claim is honest and mechanically checked, plus the closed launch gate — a **timed** Postgres restore drill, a **timed** key-ring restore drill, Semgrep/Trivy/secret scan wired into `scripts/ci`, the full mandatory suite green, and the final ToS + onboarding copy a first paying tenant will actually read.
**Status:** todo · **Size:** L · **Session:** 1 of 2 (see Size warning)
**Depends on:** P28 (must be `done`) — and, transitively, P00-P27
**Blocks:** nothing. This is the last v1 phase; what follows it is the v1 release checklist and v2 planning.

**Size warning.** This is an **L** phase and `plan/README.md` already declares its split line: **steps 1-6 = the marketing site (this file, session 1); steps 7-10 = the launch gate**. If the session clock is a single sitting, stop after step 6, create `plan/v1/P29a-launch-hardening-and-drills.md` carrying steps 7-10 verbatim (with their tests, files and risks), add its row to `plan/README.md`, and paste **its** next-session prompt instead of the one at the bottom. Do **not** split anywhere else: a site that ships before the drills is the one ordering that lets an unlaunchable claim go public.

## Prerequisites (facts, not phases)
- `scripts/ci.ps1` green on the tree as found (SESSION-PROTOCOL **O2**). Every phase P00-P28 is `done` in `plan/README.md`.
- `@wp/domain` exports `BANNED_CLAIMS`, `SAFE_MODE_DISCLAIMER` and `BROADCAST_DISCLOSURE` (P00), and `scripts/check-copy.ts` already enforces both co-presence rules with a non-zero matched-file count.
- `@wp/ui` + `@wp/design-tokens` exist and carry the Devanagari/Indic fallbacks (P05); `packages/i18n` has `en` and `hi` catalogues.
- **Gate A closed:** `docs/evidence/P10-session-cost.md` exists (per-session cost table, derived `MAX_SESSIONS_PER_WORKER`).
- **Gate B closed:** `docs/evidence/P26-*.md` exists (the measured capacity table). **Gate C is NOT closed** — no production fleet has run ≥2,000 concurrent sessions within SLO for 7 days, so the only capacity sentence permitted anywhere on this site is the Gate C wording in step 4.
- P28 shipped `admin/backend` with staff auth and `/internal/v1`. **Check before step 5:** if P28 already created the `leads` table and the public lead endpoint, extend them; if not, this phase creates them (the step is written both ways).
- **Carried from P28 (2026-09-08, written here per P28 C4 - none of it is in anyone's head):**
  - P28 did **not** create `leads` or the public lead endpoint → step 5 creates both. Next free migration number is **0074** (P28 ended at 0073; `EXPECTED_SCHEMA_VERSION = 73`).
  - v1b leftovers **not** built in P28 and **not** owed to P29: the DSAR pipeline, the retention-policy editor, Parquet archival → `plan/v2/README.md` unless the founder pulls them in. The admin audit drawer shows the audit row, not the stored `staff_audit_log.result` JSON (the admin audit contract carries no `result` field) - small follow-up, not a launch gate.
  - `pgaudit` is **not** enabled for `wp_admin_app`; `docs/RUNBOOK.md` → `## staff-accounts` states that raw `psql` by a staff member is unaudited. The launch checklist (step 10) must not claim "every read is audited" beyond the admin API.
  - **Launch-checklist rows P28 adds:** (a) confirm which role the production pool connects as - migrations 0072 (`wallet_ledger_ext_refs` UPDATE) and 0073 (`campaigns` SELECT/INSERT/UPDATE) fixed `wp_app` grants that were missing since P18/P23 and invisible because dev/test connect as the owner; if production is `wp_app`, broadcast writes and the wallet credit stamp were broken until now; the structural fix (integration suites under a non-owner, non-BYPASSRLS role) is the P06-P08 open item; (b) the admin console is deployed as its own `admin-api` process (ADR 0014) with `ADMIN_IP_ALLOWED_CIDRS` non-empty (empty = nobody can log in), `INTERNAL_API_ENABLED=true` + the shared `INTERNAL_API_SERVICE_TOKEN_SECRET` on `app/backend`, `INTERNAL_API_ALLOWED_CIDRS` naming only the admin host, and at least one staff account created via `admin/backend/src/ops/create-staff-user.ts` (`docs/RUNNING-LOCALLY.md` → "Admin console").
  - The founder decision on P26's six fleet-scale integration suites (env-gate vs stop `wp-p26-drift` vs second Redis) was still open at P28 close; the gate's `integration` step shows those six files red until it is taken. P29's own C5 will hit the same six.
  - Legacy P19 exports `internalWalletContract` (`/v1/internal/wallet/*`, zero importers) remain in `packages/contracts/src/internal/wallet.ts` pending the P28 reviewer's call; delete in P29 if the C1 notes say so.
- ADRs **0013-0020 accepted**. No production traffic and **no paying tenant yet** — that is precisely the window this phase exists to close.
- **O3:** no new design decision is needed. Website stack (Next.js 16 App Router, static export), lead-capture ownership, key-ring approach and backup posture are all decided (ADR 0016, blueprint *Surfaces* / *Infrastructure & no-git deployment*, data-and-security §4.3/§4.6). Go straight to E1. If the LCP budget forces a stack change, that is a `/decide` at C6 — see *Risks*.

## What you are building (3-6 bullets)
- `website/` — Next.js 16 App Router **static export**: hero (GSAP confined to the hero island), features, MDX docs/blog, pricing, and the legal set (ToS / DPA / privacy) carrying the **six plain-language tenant statements** and the ban-risk disclosure verbatim.
- The **copy gate extended over the whole site**: `check-copy` scans `website/**` incl. MDX and Hindi copy, asserts `SAFE_MODE_DISCLAIMER` and `BROADCAST_DISCLOSURE` co-presence, and rejects any derived capacity/cost number on a public surface (Gate C).
- The **performance gate**: LCP ≤ 2.5 s on an India 4G profile, measured in CI against the built static export, with the artefact filed.
- The **lead form** posting to a narrow, rate-limited, bot-guarded public endpoint on `admin/backend` — never the customer API.
- The **launch gate**: Semgrep + Trivy + filesystem-mode secret scan as real `scripts/ci` steps; a **timed** pgBackRest PITR restore drill; a **timed** key-ring restore drill from the offline copy; `docs/LAUNCH-CHECKLIST.md` with one evidence path per gate.
- Final **ToS + onboarding copy**, with the accepted ToS version recorded on the tenant's consent row.

## Read first (do not search — these are the canonical sources)
| What | Path | Section |
|---|---|---|
| Blueprint | `.memory/research/2026-08-25-v1-architecture-blueprint.md` | **What we can and cannot promise** (the can/cannot lists are the source of the ToS wording); **Testing strategy** (evidence rule, the copy/isolation/security suites); **Surfaces** (`website/` = Next 16 static export, LCP budget, `@wp/ui` smoke build `[R-30s]`, lead capture is admin-owned `[R-52]`); **Hardening** (Semgrep + Trivy + secret scan); **Infrastructure & no-git deployment** (pgBackRest → B2, RPO 5 min, timed restore drill before the first paying tenant, key ring in exactly three places) |
| Blueprint | `.memory/research/2026-08-25-v1-architecture-blueprint.md` | **Open questions → Acknowledgements I need on the record** (23 ban-risk posture, 24 derived capacity) — these two are ToS/onboarding content, not backlog |
| Scope delta | `.memory/research/2026-08-26-v1r-scope-delta-and-decisions.md` | **Broadcast design → the honest paragraph** (source copy for the Broadcast page, verbatim = `BROADCAST_DISCLOSURE`); **Decided: registered vs connected → The 10k posture** (the **Gate C wording**, and the rule that the cost brackets go into `check-copy`); **Groups → Required risk copy**; **Normative `desired_state` semantics** (the verbatim parked-number copy); **Wallet → Reversals** (the no-refund-after-ack line belongs on the pricing page); **Pricing and top-up** (prices are placeholders until the founder sets them) |
| ADR | `.memory/decisions/0016-v1-v2-scope-split.md` | website last; no capacity number and no price quoted before the measurement gate |
| ADR | `.memory/decisions/0018-ten-thousand-session-target-memory-budget-and-connected-unit.md` | connected vs registered as the sold unit; Gate C |
| ADR | `.memory/decisions/0019-wallet-and-per-message-metering.md` | §9 pricing/top-up — what the pricing page may and may not say |
| ADR | `.memory/decisions/0003-no-git-no-vcs-linkage.md` | all — decides the secret-scanner mode in step 7 |
| ADR | `.memory/decisions/0009-infrastructure-topology-and-no-git-deployment.md` | the restore-drill requirement, RTO 60 min / RPO 5 min at launch tier |
| ADR | `.memory/decisions/0014-repo-structure-shared-packages-and-conventions.md` | `website/` layout and the `packages/* ↛ apps` rule |
| Design | `.memory/research/2026-08-25-v1-design-data-and-security.md` | §4.3 key ring (3 copies), §4.6 rotation and loss (quarterly drill), §7 privacy — the retention table and **the six plain-language tenant statements** (around line 800) |
| Design | `.memory/research/2026-08-25-v1-design-repo-structure.md` | §1.3 `website/` tree, §2.2 dependency rules, §2.3 guard 7 (`check-copy`) |
| Phase file | `plan/v1/P00-workspace-guards-and-domain.md` | steps 3 and 9 — the guard registry contract and how `check-copy` is wired |
| Phase file | `plan/v1/P01-server-kit-and-crypto.md` | *Risks* — "production key-ring provisioning, the 3-copy rule and the restore drill are P29" |
| Invariants | `.claude/rules/core-invariants.md` | **6** (no evasion — the copy half) and **7** (tests are evidence) especially |
| Safety | `.claude/skills/safety-compliance/SKILL.md` | **Honest product claims** — this phase is where that section is either enforced or quietly broken |
| Path rules | `.claude/rules/api.md` | all (the lead endpoint) |

## Dispatch plan (written at session open 2026-09-08, per SESSION-PROTOCOL E1 - this file predated dispatch plans)
Session 1 covers steps 1-6 (the split line). Tests are written red-first inside the unit that owns the step.
Layout decision (main session): the Next.js app lives under `website/src/{app,components,lib,content/copy,styles}` and MDX under
`website/content/**`, so every existing guard glob (`website/src/**` in tenant-scope, raw-hex, eslint guards, depcruise) keeps
matching without edits; `website/out/**` and `**/.next/**` are build artefacts and are excluded from every scan.
- **Wave 1 (parallel, disjoint trees):**
  - U1 `implementer` - guard + CI wiring (steps 1/4 guard half): `scripts/check-copy.ts` (+ sibling `scripts/guards/copy-public-capacity-lib.ts`), `scripts/guards/check-copy-website.test.ts` + fixtures, `scan-config.ts` artefact exclusions (+ `meta.test.ts` pin), `scripts/ci-steps.ts` (`website-build`, `website-lcp`), root `vitest.config.ts` perf/e2e excludes, `.prettierignore` / eslint ignores / depcruise exclude for `website/out` + `.next`.
  - U2 `ui-implementer` - static-export scaffold (step 2): `website/{package.json,next.config.mjs,tsconfig.json,postcss.config.mjs}`, `src/app/{layout,page}.tsx`, `src/app/(marketing)/{features,pricing}/page.tsx`, `src/components/hero/*`, `src/components/ui-smoke.tsx`, `src/lib/{seo,analytics,disclosures}.ts`, `src/content/copy/*.ts`, `src/styles/globals.css`, `website/tests/{ui-smoke,analytics}.test.ts`.
  - U4a `db-engineer` - `leads` storage (step 5 schema half): `db/migrations/0074_leads.sql`, `db/schema/leads.ts` (+ `SCHEMA_TABLES`), `ISOLATION_NON_TENANT_TABLES`, `EXPECTED_SCHEMA_VERSION = 74`, grants snapshot regenerated, P28 admin write-set pin extended. Contains a migration - runs in wave 1 only because the other two units touch no db/ file and no test that reads the schema.
- **Wave 2 (parallel, after wave 1):**
  - U3 `ui-implementer` - MDX pipeline + docs/blog/legal content + legal pages (steps 3/4 content half): `src/lib/content.ts`, `src/app/(marketing)/{docs,blog,legal}/**`, `website/content/**/*.mdx`, `website/tests/{legal,content}.test.ts`.
  - U4b `implementer` - lead endpoint + form + e2e (step 5 code half): `admin/backend/src/modules/leads/*`, `admin/backend/src/{server,index}.ts`, `platform/config.ts`, `website/src/lib/leads.ts`, `src/components/lead-form.tsx`, `src/app/(marketing)/contact/page.tsx`, `website/playwright.config.ts`, `website/tests/e2e/*`, `docs/RUNNING-LOCALLY.md`.
  Shared contracts: U3 and U4b both add routes under `src/app/(marketing)/` but never the same file; neither edits U2's `layout.tsx`/`page.tsx`/pricing page (pricing links to `/contact/`, which U4b creates).
- **Wave 3:** U5 `implementer` - the performance gate (step 6): `website/lighthouserc.json`, `tests/perf/{india-4g.json,lcp-budget.test.ts}`, `vitest.perf.config.ts`, `docs/evidence/P29-lcp-india4g.md` (+ raw JSON under `docs/measurements/`).
- Then E3 (test-engineer edge pass with the C2 checklist, test-runner full unit suite), C1 reviewer, C3-C7. Steps 7-10 → `P29a` (see Size warning).

## Ordered minimum steps
Migration numbers: use the next free 4-digit numbers after P28 and write the real names into the files list.

- [x] 1. **Write the failing tests and gates first** (all red, none skipped, none `.only`) *(session 1: every test for steps 2-6 was written red-first inside the unit that owns the step — `website/tests/{legal,ui-smoke,content,leads,analytics}.test.ts`, `website/tests/perf/lcp-budget.test.ts`, `website/tests/e2e/lead-form.spec.ts`, `admin/backend/src/modules/leads/leads.routes.test.ts` (+ `leads.repo.integration.test.ts`), `scripts/guards/check-copy-website.test.ts`, `db/tests/leads-schema.test.ts`; the step 7-10 tests — `security-scan`, `restore-drill`, `keyring-restore`, `launch-checklist` — are carried verbatim into `plan/v1/P29a-launch-hardening-and-drills.md`)* → `website/tests/legal.test.ts`, `website/tests/ui-smoke.test.ts`, `website/tests/perf/lcp-budget.test.ts`, `website/tests/e2e/lead-form.spec.ts`, `admin/backend/src/modules/leads/leads.routes.test.ts`, `scripts/guards/check-copy.test.ts` (extend), `scripts/guards/security-scan.test.ts`, `infra/backup/__tests__/restore-drill.test.ts`, `infra/backup/__tests__/keyring-restore.test.ts`, `docs/__tests__/launch-checklist.test.ts`.
- [x] 2. **Scaffold the static export and prove the `@wp/ui` boundary** *(done 2026-09-08, U2: layout under `website/src/**`; `next build --webpack` + `extensionAlias` + `modularizeImports('@wp/ui')` were needed — see deviations in the session log)* → `website/next.config.mjs` (`output: 'export'`, `images.unoptimized: true`, no route handlers, no server actions), `website/app/{layout.tsx,page.tsx}`, `website/app/(marketing)/{features,pricing}/page.tsx`, `website/components/hero/{hero.tsx,hero-motion.tsx}` (GSAP/Lenis **only** inside `hero-motion.tsx`, `'use client'`, dynamically imported, `prefers-reduced-motion` honoured), `website/lib/seo.ts`, `website/styles/globals.css` consuming `@wp/design-tokens` (a raw hex in `website/` is a lint error, same rule as `packages/ui`). Add `pnpm -F website build` + the three-component `@wp/ui` smoke import to `scripts/ci-steps.ts` — without it the first Button import into a Server Component fails opaquely (`[R-30s]`).
- [x] 3. **MDX content pipeline + docs/blog** *(done 2026-09-08, U3: next-mdx-remote/rsc + gray-matter + remark-gfm, 7 docs incl. `hi/safe-mode`, 1 post)* → `website/content/{docs,blog}/*.mdx`, `website/app/(marketing)/docs/[...slug]/page.tsx`, `website/app/(marketing)/blog/[slug]/page.tsx`, `website/lib/content.ts` (filesystem read at build time only — **no CMS process on the prod box**). Content must include the honest operational pages a tenant will look for: *How sending is paced*, *What Safe Mode does and does not do*, *What "Broadcast" means*, *Groups and why they are capped*, *Parked numbers*, *Wallet and billing*.
- [x] 4. **The honest-copy layer, and the guard that keeps it honest** *(done 2026-09-08, U1 + U3: clause (e) in `check-copy` with N read live from `docs/capacity/fleet-capacity.md`; ToS statement 6 deliberately amended — self-service export/deletion is not built, see session log)* → `website/content/legal/{terms,privacy,dpa}.mdx` + `website/app/(marketing)/legal/{terms,privacy,dpa}/page.tsx`. Legal + product copy must carry, **verbatim from the sources**: the six plain-language tenant statements (data-and-security §7), the ban-risk posture (blueprint ack 23), `SAFE_MODE_DISCLAIMER`, `BROADCAST_DISCLOSURE`, the group risk paragraph, the parked-number caveat, the wallet no-refund-after-ack line, and the ambiguous-send policy. Then extend `scripts/check-copy.ts`: glob `website/**/*.{tsx,ts,mdx,md,json}` (assert a **non-zero matched-file count**), the two co-presence rules, the Hindi/Hinglish banned list, and a **new rule** rejecting any of the derived capacity/cost figures (`10,000`, `2,000 sessions`, `$0.2x`, `18 MB`, the bracket tables) on a public surface unless the string is exactly the Gate C sentence: *"the architecture has no known ceiling below 10,000 concurrent connected numbers and has been measured to N"*, with **N** filled from `docs/evidence/P26-*.md`. Constants are imported from `@wp/domain` — the website never re-types a disclosure.
- [x] 5. **Lead capture on `admin/backend`, never the customer API** *(done 2026-09-08, U4a + U4b: migration `0074_leads.sql`, `POST /public/v1/leads`, in-process token bucket with real 429 headers, honeypot + timing guard, HMAC ip_hash, Playwright e2e 2/2)* → if `leads` does not already exist: `db/migrations/00NN_leads.sql` + `db/schema/leads.ts` (`id, name, email, company, phone_e164, message, source, utm jsonb, ip_hash, created_at`), registered in `isolation_non_tenant_tables` with the reason "marketing lead, no tenant exists yet" and in `db/test/role-grants.snapshot.json`. Then `admin/backend/src/modules/leads/{leads.repo.ts,bot-guard.ts,leads.routes.ts}` — `POST /public/v1/leads`, Zod `.strict()`, per-IP + global token bucket returning a **real 429 with headers**, honeypot + timing bot guard, no auth, no tenant context, CORS limited to the site origin; and `website/components/lead-form.tsx` + `website/lib/leads.ts` posting to it. `check-tenant-scope` must show this module touching **no** tenant table.
- [x] 6. **Close the performance gate** *(done 2026-09-08, U5 + U5b: Lighthouse 13.4.1 simulated India-4G; the first run missed the budget on every route (2.7-2.85 s) because the measurement server sent identity-encoded bytes — fixed the harness to serve gzip/brotli like the production edge, compression proven in the trace; compressed LCP 0.9-1.9 s on all three routes, hero island restored with GSAP in a lazy chunk; evidence `docs/evidence/P29-lcp-india4g.md` incl. the uncompressed FAIL control; edge compression is now a P29a launch-checklist row)* → `website/lighthouserc.json` + `website/tests/perf/india-4g.json` (throttling profile: ~1.6 Mbps down / 750 kbps up / 150 ms RTT, mid-tier mobile CPU multiplier 4×), a CI step that builds the export, serves it statically and runs Lighthouse against `/`, `/pricing` and one docs page, asserting **LCP ≤ 2.5 s** and failing the build otherwise. File `docs/evidence/P29-lcp-india4g.md` with the verbatim run output for all three routes. **If the hero misses the budget, cut the animation — do not raise the budget** (blueprint: the hero is the first thing to revisit).
- [ ] 7. **Supply-chain and secret scanning as real CI steps** → `.semgrep.yml` (pinned ruleset + our own rules: no `child_process` in `app/backend/src/roles/api.ts`, no `certificate verification disabled`, no `Math.random` in a security path), `infra/deploy/trivy.yaml` (image + filesystem scan, `--exit-code 1` on HIGH/CRITICAL, pinned DB version, fail **loudly** on a DB download failure), `scripts/scan-secrets.ts` running the scanner in **filesystem mode** (`gitleaks dir` / `trufflehog filesystem`) — **never** a git-history mode and never `git init` for a tool's benefit (ADR 0003). Register all three in `scripts/guards/registry.ts` and `scripts/ci-steps.ts` with fixtures under `scripts/guards/__fixtures__/` proving each one actually fails on a seeded finding. File `docs/evidence/P29-security-scans.md`.
- [ ] 8. **Key-ring provisioning + the timed key-ring restore drill** → `infra/backup/keyring-restore-drill.ts` + `docs/runbooks/key-ring-restore.md`. The drill: seal a real `initAuthCreds()`-shaped record under the production-shaped key ring in a **scratch** environment, destroy the running copy, restore the ring from the **offline** copy, assert byte-identical plaintext, and record wall-clock duration. Also record where the three copies live (host secret store / founder's offline encrypted copy / sealed second offline copy) **by description, never by content** — no key material, path secret or passphrase in any file, log, test snapshot or evidence artefact. File `docs/evidence/P29-keyring-restore-drill.md` with the timing and the operator's name. Set the quarterly cadence reminder in the runbook.
- [ ] 9. **The timed Postgres restore drill** → `infra/backup/{pgbackrest.conf.example,backup-cron.md,restore-drill.ps1,restore-drill.sh}` + `docs/runbooks/restore-from-backup.md`. The drill restores a full + WAL PITR into a **scratch container on a scratch port** (the script hard-refuses any target whose DSN matches the production host — that check is a named test), then asserts: schema version equals the current migration, `SELECT count(*)` parity on `message_jobs`, `wallet_ledger`, `messages` and `contacts` against the source snapshot, ledger continuity (`balance_after_minor` chain) intact, and the recovery point within the RPO. Record **measured RTO and RPO** against the stated targets (RPO 5 min; RTO ~60 min at the launch tier, and state the delta honestly if it misses). File `docs/evidence/P29-restore-drill.md`.
- [ ] 10. **The launch checklist, the final ToS/onboarding copy, and the full suite** → `docs/LAUNCH-CHECKLIST.md`: one row per gate with its evidence path — Gate A (P10), Gate B (P26), restore drill, key-ring drill, three scans clean, mandatory send-path suite 1-23 green, Safe Mode suite green, isolation A/B/C green, log-grep PII test green, `no_forbidden_mechanism_exists` green, copy suite green incl. Hindi, LCP artefact, real prices set (or the pricing page in "contact us" mode), backups running with a verified schedule, alerting reachable, runbooks present. Wire the final ToS version into onboarding: `app/frontend/src/features/onboarding/components/consent-step.tsx` renders the six statements + the ban-risk disclosure and records `tos_version` on the existing consent/audit row (one additive migration only if a column is genuinely missing). Then run `scripts/ci.ps1` end to end and paste the verbatim tail.

## Tests that prove it
| Test file | Case | Asserts |
|---|---|---|
| `website/tests/legal.test.ts` | `the_six_plain_language_statements_appear_verbatim_in_the_tos` | each of the six strings from data-and-security §7 is present character-for-character in `content/legal/terms.mdx` |
| `website/tests/legal.test.ts` | `the_ban_risk_disclosure_is_present_in_both_the_tos_and_onboarding` | the "WP's pacing controls reduce the risk … cannot prevent or guarantee against WhatsApp restrictions" sentence exists on both surfaces |
| `website/tests/legal.test.ts` | `the_website_imports_disclosures_from_domain_and_never_retypes_them` | source scan: no near-duplicate of `SAFE_MODE_DISCLAIMER`/`BROADCAST_DISCLOSURE` literal in `website/` outside the import |
| `website/tests/legal.test.ts` | `the_pricing_page_quotes_no_capacity_number_and_no_placeholder_price` | no bracket figure, no `₹0.15`-style placeholder unless `PRICES_CONFIRMED` is set; otherwise the page renders "contact us" |
| `scripts/guards/check-copy.test.ts` | `website_content_is_scanned_with_a_non_zero_matched_file_count` | the new glob matches ≥ 1 real file; exclusions unchanged |
| `scripts/guards/check-copy.test.ts` | `a_banned_claim_in_hindi_marketing_copy_is_rejected` | seeded `hi` MDX fixture with "ban nahi hoga" / "100% safe" fails the scan |
| `scripts/guards/check-copy.test.ts` | `a_broadcast_page_without_the_broadcast_disclosure_is_rejected` | co-presence guard fires on a fixture page mentioning "Broadcast" |
| `scripts/guards/check-copy.test.ts` | `a_safe_mode_page_without_the_disclaimer_is_rejected` | same for `SAFE_MODE_DISCLAIMER` |
| `scripts/guards/check-copy.test.ts` | `a_derived_capacity_or_cost_number_on_a_public_page_is_rejected` | fixture containing "10,000 concurrent sessions" or "$0.28/number" fails; the exact Gate C sentence with N filled passes |
| `website/tests/ui-smoke.test.ts` | `three_wp_ui_components_build_inside_the_static_export` | a Server Component importing three `@wp/ui` components builds; a missing `'use client'` fails the build, loudly |
| `website/tests/perf/lcp-budget.test.ts` | `home_lcp_is_under_2500ms_on_the_india_4g_profile` | Lighthouse against the built export; hard fail above 2,500 ms |
| `website/tests/perf/lcp-budget.test.ts` | `pricing_and_docs_pages_are_under_the_same_budget` | the budget is not hero-only |
| `website/tests/perf/lcp-budget.test.ts` | `the_hero_animation_is_not_on_the_critical_path` | GSAP/Lenis are dynamically imported and absent from the initial bundle graph |
| `website/tests/e2e/lead-form.spec.ts` | `a_lead_submission_reaches_admin_backend_and_returns_a_confirmation` | Playwright end-to-end against the built export + `admin/backend` |
| `website/tests/e2e/lead-form.spec.ts` | `the_lead_form_never_posts_to_the_customer_api_origin` | network assertion: no request to the `app/backend` origin (`[R-52]`) |
| `admin/backend/src/modules/leads/leads.routes.test.ts` | `a_public_lead_post_is_rate_limited_with_a_real_429_and_headers` | burst → 429 with limit headers; the limiter is not `skip: () => true` |
| `admin/backend/src/modules/leads/leads.routes.test.ts` | `the_honeypot_and_timing_bot_guard_reject_without_writing_a_row` | 0 rows written, counted |
| `admin/backend/src/modules/leads/leads.routes.test.ts` | `the_lead_endpoint_reads_and_writes_no_tenant_table` | `check-tenant-scope` over the module + a runtime assertion of the touched-table set |
| `admin/backend/src/modules/leads/leads.routes.test.ts` | `a_lead_row_stores_no_raw_ip_and_no_unbounded_free_text` | `ip_hash` only; message length capped |
| `scripts/guards/security-scan.test.ts` | `semgrep_trivy_and_secret_scan_are_registered_ci_steps` | all three appear in `scripts/ci-steps.ts` and in the guard registry |
| `scripts/guards/security-scan.test.ts` | `each_scanner_fails_on_its_seeded_fixture` | a planted hardcoded key, a HIGH-CVE base image pin and a `rejectUnauthorized:false` each produce a non-zero exit |
| `scripts/guards/security-scan.test.ts` | `the_secret_scanner_runs_in_filesystem_mode_and_never_invokes_git` | argv assertion + a spy proving no `git` process is spawned (ADR 0003) |
| `scripts/guards/security-scan.test.ts` | `a_scanner_that_cannot_fetch_its_database_fails_the_build` | simulated offline DB → non-zero exit, never a silent pass |
| `infra/backup/__tests__/restore-drill.test.ts` | `the_restore_drill_refuses_a_production_target` | a production-looking DSN → immediate refusal, nothing executed |
| `infra/backup/__tests__/restore-drill.test.ts` | `a_drill_run_asserts_schema_version_and_row_count_parity` | restored scratch cluster matches the source snapshot on the four named tables |
| `infra/backup/__tests__/restore-drill.test.ts` | `a_drill_run_writes_a_timed_evidence_file_with_measured_rto_and_rpo` | evidence file exists with start/end timestamps and both measured numbers |
| `infra/backup/__tests__/restore-drill.test.ts` | `the_restored_wallet_ledger_chain_is_continuous` | `balance_after_minor` continuity holds after restore — money survives a restore or the restore is not done |
| `infra/backup/__tests__/keyring-restore.test.ts` | `a_sealed_record_opens_after_restoring_the_offline_key_ring_copy` | byte-identical plaintext, in a scratch environment |
| `infra/backup/__tests__/keyring-restore.test.ts` | `the_drill_never_prints_or_persists_key_material` | stdout, evidence file and test snapshots contain no base64 key, no passphrase, no secret path |
| `docs/__tests__/launch-checklist.test.ts` | `every_launch_gate_row_names_an_evidence_file_that_exists` | the checklist cannot be ticked without an artefact on disk |
| `docs/__tests__/launch-checklist.test.ts` | `no_capacity_or_price_claim_ships_before_its_gate_evidence_exists` | Gate B artefact required before any capacity sentence renders; Gate C required before a 10,000 claim |

Mandatory-suite tests this phase makes green: **none new from the blueprint's numbered send-path table (1-23)** — those are already green from P03-P13 and must stay green. This phase closes the blueprint's *Isolation, security and copy suites* row: `copy_contains_no_banned_claims` (incl. Hindi/Hinglish) with the `SAFE_MODE_DISCLAIMER` co-presence assertion, now extended across `website/`, plus the real-429 rate-limit test for the public lead route class.

## Definition of done
- [x] Every step box above is ticked (steps 1-6; steps 7-10 carried to P29a).
- [x] *(session 1: gate 2026-09-08 22:43 IST on `wp_test2` — 29 steps + unit 533/533 + db 56/56 green; integration 596/602 with the six known P26 fleet suites red; `build`/`website-build`/`website-lcp` green standalone — tail in the session log)* `scripts/ci.ps1` output pasted **verbatim** into the session log — green, including the three new scan steps and the LCP step (session 1: UNTICKED; steps 7-10 CI in P29a C5).
- [x] Named tests above exist and pass; no test is skipped or `.only` (steps 1-6; step 7-10 tests carried to P29a).
- [ ] `docs/evidence/P29-lcp-india4g.md`, `docs/evidence/P29-security-scans.md`, `docs/evidence/P29-restore-drill.md` and `docs/evidence/P29-keyring-restore-drill.md` all exist, verbatim, each with its measured numbers (only docs/evidence/P29-lcp-india4g.md exists; security-scans / restore-drill / keyring-restore-drill are P29a).
- [ ] `docs/LAUNCH-CHECKLIST.md` exists and every row either points at an existing artefact or is explicitly marked **NOT DONE** — a blank row is a failure, not a pass (P29a step 10).
- [x] `reviewer` verdict recorded: APPROVED (or APPROVED-with-notes, notes filed) (APPROVED-with-notes, re-review 2026-09-08, notes filed in the session log).
- [x] Invariant check done (SESSION-PROTOCOL C3) with no unresolved finding — invariant **6** answered explicitly for every new public string (C3 in the session log; invariant 6 answered for every new public string).
- [x] Files created/changed listed below (this list *is* the diff — there is no git).

## Files created or changed this session
<!-- fill during the session; the reviewer reviews exactly this list -->
### Session 1 (2026-09-08) — actual list, appended per wave
**Wave 1 — U1 guards/CI (implementer):** `scripts/check-copy.ts` (clause e, `scanCopy(files, claims, { measuredN })`, `filesScanned`), NEW `scripts/guards/copy-public-capacity-lib.ts`, NEW `scripts/guards/check-copy-website.test.ts`, NEW fixtures `scripts/guards/__fixtures__/copy/{hindi-marketing-banned,website-broadcast-no-disclosure,website-safe-mode-no-disclaimer}.mdx`, `scripts/guards/scan-config.ts` (ARTIFACT_EXCLUSIONS + `website/out/**`, `**/.next/**`), `scripts/guards/meta.test.ts` (pin), `scripts/ci-steps.ts` (`website-build`, `website-lcp`), root `vitest.config.ts` (exclude `website/tests/perf/**`, `website/tests/e2e/**`), `.prettierignore`, `packages/config/eslint.config.js` (ignores), `.dependency-cruiser.cjs` (exclude).
**Wave 1 — U2 website scaffold (ui-implementer):** `website/package.json` (main session then re-wrote it: `main` restored to `./src/app/layout.tsx`, wave 2/3 deps pre-added), `website/tsconfig.json` (module ESNext + moduleResolution bundler, jsx preserve, rootDir `.`), `website/next.config.mjs` (`output: 'export'`, `--webpack` build, `resolve.extensionAlias`, `modularizeImports` for `@wp/ui`), `website/postcss.config.mjs`, `website/src/styles/globals.css`, `website/src/lib/{seo,analytics,disclosures}.ts`, `website/src/content/copy/{shared,home,features,pricing}.ts`, `website/src/components/ui-smoke.tsx`, `website/src/components/hero/{hero,hero-motion-island,hero-motion}.tsx`, `website/src/app/layout.tsx`, `website/src/app/page.tsx`, `website/src/app/(marketing)/{features,pricing}/page.tsx`, `website/tests/{ui-smoke,analytics}.test.ts`, `pnpm-lock.yaml`; DELETED `website/src/index.ts`.
**Wave 1 — U4a leads schema (db-engineer):** NEW `db/migrations/0074_leads.sql`, NEW `db/schema/leads.ts`, `db/schema/index.ts` (SCHEMA_TABLES + export), `db/src/isolation/tenant-tables.ts` (`leads` in ISOLATION_NON_TENANT_TABLES), `db/src/schema-version.ts` (74), `db/schema/grants.snapshot.json` (regenerated), `db/tests/grants-snapshot-p28-admin-role.test.ts` (pinned write set + `leads` INSERT), NEW `db/tests/leads-schema.test.ts`.
**Wave 2 — U3 MDX + content + legal (ui-implementer):** NEW `website/src/lib/content.ts`, NEW `website/src/lib/mdx-components.tsx`, NEW `website/src/content/copy/docs.ts`, NEW `website/src/app/(marketing)/docs/page.tsx`, NEW `website/src/app/(marketing)/docs/[...slug]/page.tsx`, NEW `website/src/app/(marketing)/blog/page.tsx`, NEW `website/src/app/(marketing)/blog/[slug]/page.tsx`, NEW `website/src/app/(marketing)/legal/{terms,privacy,dpa}/page.tsx`, NEW `website/content/docs/{how-sending-is-paced,what-safe-mode-does-and-does-not-do,what-broadcast-means,groups-and-why-they-are-capped,parked-numbers,wallet-and-billing}.mdx`, NEW `website/content/docs/hi/safe-mode.mdx`, NEW `website/content/blog/why-we-do-not-promise-deliverability.mdx`, NEW `website/content/legal/{terms,privacy,dpa}.mdx`, NEW `website/tests/{legal,content}.test.ts`.
**Wave 2 — U4b lead endpoint + form + e2e (implementer):** NEW `admin/backend/src/modules/leads/{bot-guard,public-rate-limit,leads.repo,leads.routes,leads-routes-test-support}.ts`, NEW `admin/backend/src/modules/leads/{leads.routes.test,leads.repo.integration.test}.ts`, `admin/backend/src/server.ts` (optional `leads` dep), `admin/backend/src/index.ts` (wiring), `admin/backend/src/platform/config.ts` (`LEADS_ALLOWED_ORIGINS`, `LEADS_IP_HASH_SECRET`), `admin/backend/src/platform/platform-read.ts` (header: `leads` in the write set), NEW `website/src/lib/leads.ts`, NEW `website/src/components/lead-form.tsx`, NEW `website/src/app/(marketing)/contact/page.tsx`, NEW `website/src/content/copy/contact.ts`, NEW `website/tests/leads.test.ts`, NEW `website/playwright.config.ts`, NEW `website/tests/e2e/{lead-form.spec.ts,serve-out.mjs}`, `docs/RUNNING-LOCALLY.md` (“Website + lead form”).
**Wave 3 — U5/U5b LCP gate (implementer):** NEW `website/lighthouserc.json`, NEW `website/vitest.perf.config.ts`, NEW `website/tests/perf/{india-4g.json,lcp-budget.test.ts,lighthouse-runner.ts,lighthouse-evidence.ts}`, `website/tests/e2e/serve-out.mjs` (gzip/brotli content negotiation), `website/src/components/hero/hero.tsx` (island dropped, then restored once the budget held), `website/src/styles/globals.css` (CSS-only panel reveal), NEW `docs/evidence/P29-lcp-india4g.md`, NEW `docs/measurements/2026-09-08-lcp-india4g.json`.
**E3 — test-engineer edge pass (C2 checklist, 65 cases, no production defect):** NEW `admin/backend/src/modules/leads/{public-rate-limit.test,leads-edge-cases.test,leads-edge-cases-2.test}.ts`, NEW `scripts/guards/check-copy-public-capacity-edge.test.ts`, NEW `website/tests/{content-edge,leads-edge}.test.ts`.
**C1 fix round (implementer, after the reviewer's CHANGES-REQUIRED):** `admin/backend/src/modules/leads/leads.routes.ts` (utm byte-size refine; limiter moved to a route-scoped `onRequest` hook; `bodyLimit` 16 KB), `admin/backend/src/modules/leads/public-rate-limit.ts` (hard cap on the bucket map + `size`; trust-proxy limitation documented), NEW `admin/backend/src/modules/leads/leads-utm-bounds.test.ts`, `admin/backend/src/modules/leads/{leads-edge-cases,leads-edge-cases-2,public-rate-limit}.test.ts`, `admin/backend/src/modules/leads/leads.repo.integration.test.ts` (42501 UPDATE/DELETE negative), `admin/backend/src/platform/http/route-policy.ts` (optional `bodyLimit`), `admin/backend/src/platform/config.ts` (`LEADS_ALLOWED_ORIGINS` required in production), NEW `admin/backend/src/platform/config-leads.test.ts`, `db/tests/leads-schema.test.ts` (utm boundary at storage), `website/content/docs/how-sending-is-paced.mdx` (08:00-20:00 default window), `website/src/lib/leads.ts` (customer-API origin/port refusal), `website/tests/leads.test.ts`, `website/src/content/copy/{shared,pricing}.ts` (`PRICING_MODE` removed — contact-us is the only mode this release), `website/tests/legal.test.ts`, `website/tests/perf/lcp-budget.test.ts` (home measured once in `beforeAll`), `website/tests/e2e/serve-out.mjs` (root-prefix guard), `docs/RUNNING-LOCALLY.md` (trust-proxy note).
**Re-review notes applied (main session):** `admin/backend/src/modules/leads/leads.routes.ts` (CORS header on the 429 from the `onRequest` hook; comments on the Fastify-native 413 and on oversized bodies spending a token), `admin/backend/src/modules/leads/leads-edge-cases-2.test.ts` (+ `a_429_from_an_allowed_origin_carries_the_cors_header_so_the_browser_can_read_it`), `.prettierignore` (the two machine-written LCP evidence files); stray scratch artefacts `admin/backend/src/modules/leads/zz-probe.test.ts` (reviewer probe), `website/lh-debug.mjs` and `.scratch-perf-before.log` (LCP unit) were moved out of the repo.
**Main session:** `plan/README.md` (P29 claimed + P29a row), NEW `plan/v1/P29a-launch-hardening-and-drills.md`, this file (Dispatch plan, this list), `website/package.json` (main restored, wave 2/3 deps), `website/content/legal/terms.mdx` (§12 rewritten: `tos_version` recording is not built yet — the previous sentence was a false claim), `website/content/legal/privacy.mdx` (analytics IP sentence made accurate for a third-party endpoint).
### Original template list (kept for the reviewer's cross-check)
- `website/next.config.mjs`, `website/package.json`, `website/tsconfig.json` — created/changed
- `website/app/layout.tsx`, `website/app/page.tsx` — created
- `website/app/(marketing)/{features,pricing}/page.tsx` — created
- `website/app/(marketing)/docs/[...slug]/page.tsx`, `website/app/(marketing)/blog/[slug]/page.tsx` — created
- `website/app/(marketing)/legal/{terms,privacy,dpa}/page.tsx` — created
- `website/content/legal/{terms,privacy,dpa}.mdx` — created
- `website/content/docs/*.mdx`, `website/content/blog/*.mdx` — created
- `website/components/hero/{hero.tsx,hero-motion.tsx}`, `website/components/lead-form.tsx` — created
- `website/lib/{content.ts,seo.ts,analytics.ts,leads.ts}` — created
- `website/styles/globals.css` — created
- `website/lighthouserc.json`, `website/tests/perf/india-4g.json` — created
- `website/tests/{legal.test.ts,ui-smoke.test.ts,perf/lcp-budget.test.ts,e2e/lead-form.spec.ts}` — created
- `db/migrations/00NN_leads.sql`, `db/schema/leads.ts` — created **only if P28 did not**
- `db/test/isolation-suite-a.test.ts` — changed: `leads` in `isolation_non_tenant_tables` with a reason
- `db/test/role-grants.snapshot.json` — changed: `leads`
- `admin/backend/src/modules/leads/{leads.repo.ts,bot-guard.ts,leads.routes.ts,leads.routes.test.ts}` — created
- `scripts/check-copy.ts` — changed: `website/**` glob, the Gate C capacity rule
- `scripts/guards/check-copy.test.ts` — changed: five new cases
- `scripts/guards/security-scan.test.ts` — created
- `scripts/guards/registry.ts`, `scripts/ci-steps.ts`, `scripts/ci.ps1`, `scripts/ci.sh` — changed: website build + smoke, LCP, semgrep, trivy, secret scan
- `scripts/scan-secrets.ts` — created
- `.semgrep.yml`, `infra/deploy/trivy.yaml` — created
- `scripts/guards/__fixtures__/**` — changed: scanner and copy fixtures
- `infra/backup/{pgbackrest.conf.example,backup-cron.md,restore-drill.ps1,restore-drill.sh,keyring-restore-drill.ts}` — created
- `infra/backup/__tests__/{restore-drill.test.ts,keyring-restore.test.ts}` — created
- `docs/runbooks/{restore-from-backup.md,key-ring-restore.md}` — created
- `docs/RUNBOOK.md` — changed: links to the two new runbooks
- `docs/LAUNCH-CHECKLIST.md`, `docs/__tests__/launch-checklist.test.ts` — created
- `docs/evidence/P29-{lcp-india4g,security-scans,restore-drill,keyring-restore-drill}.md` — created
- `app/frontend/src/features/onboarding/components/consent-step.tsx` — changed: six statements + ban-risk disclosure + `tos_version`
- `packages/i18n/src/catalogues/{en,hi}.ts` — changed: final ToS/onboarding copy keys
- `plan/README.md` — changed: P29 row → `done` (at C6)

## Risks / gotchas specific to this phase
- **Static export has no server.** `output: 'export'` forbids route handlers, server actions, middleware, ISR and dynamic image optimisation. The lead form therefore posts **cross-origin** to `admin/backend` — plan the CORS preflight and the origin allow-list up front. The first person to "just add an API route" silently breaks `next build` in CI; the smoke build step is what catches it.
- **The LCP budget is the gate, not a suggestion.** GSAP + Lenis on an India 4G profile is exactly the combination that misses 2.5 s. Fix order: dynamic-import the motion island → drop Lenis → static hero image with a CSS-only reveal → cut the animation. Only if all four fail is a stack change (Astro) on the table, and that is a `/decide` ADR at C6, never a silent budget raise.
- **This is the phase where honest claims are either enforced or quietly broken.** No "ban-proof", "won't get blocked", "avoid ban", "instant bulk", "guaranteed delivery", "100% safe", "ban nahi hoga", "block nahi hoga" — in **any** language, including a testimonial, an OG description, a JSON-LD blob, an alt text or an MDX blog post. Marketing pressure lands here and the answer is the guard, not judgement.
- **Capacity and price are gated by evidence, not by confidence.** Gate C is not closed. The only permitted capacity sentence is the Gate C wording with N taken from the P26 artefact. The cost brackets ($0.23-$0.43/connected/month, 18/38 MB, the box tables) are **internal**; `check-copy` must reject them on a public surface. If the founder has not set real per-message prices (delta open question 1), the pricing page ships in "contact us" mode — placeholders never go public.
- **The Broadcast page is a claims trap.** Say what we actually do: individually-addressed, paced messages, hours or days for a large audience, the pacing is the product, and a broadcast never raises a cap. The honest paragraph in the scope delta is the source copy and `BROADCAST_DISCLOSURE` co-presence is enforced — do not paraphrase it into something shorter and punchier.
- **The group page must carry the risk paragraph.** Group blasting is the highest-report-rate behaviour available; the copy says so, states that we cap it, disable it in warm-up and switch it off first, and states plainly that no pacing changes that.
- **Secret scanning without git is a real trap.** `gitleaks` defaults to a git-history scan and will fail or try to initialise a repo. Use directory/filesystem mode explicitly, assert it in a test, and never run `git` for any reason (ADR 0003). A scanner that errors and returns 0 is worse than no scanner — assert non-zero exit on failure.
- **A scan or a drill that "passes" without running is the failure mode.** Every scanner gets a fixture that must make it fail; every drill writes a timed artefact. The guard meta-assertion applies: a step matching zero files, or a drill with no evidence file, is not a step.
- **The restore drill must never touch production.** Scratch container, scratch port, scratch data directory, and a hard refusal on a production-shaped DSN with a test to prove it. Also verify the **backup encryption passphrase is not the KEK ring passphrase** — two separate compromises is the entire point.
- **Key material discipline during the key drill.** Never print, log, snapshot or paste key bytes, passphrases or the secret storage path. Record locations by description. If the drill fails, the honest outcome is "the key backup does not work and launch is blocked", not a workaround.
- **RTO honesty.** The blueprint's flat 60 min holds at the launch tier only; the scope delta amends it to 4-6 h from backup at 10k with replica promotion as the real path. Write the **measured** number and the tier it was measured at; do not publish an aspirational RTO.
- **Analytics carries no PII.** No phone number, email or tenant id in a query string, event property or URL; a privacy-respecting, cookie-light setup only, and the privacy page must describe exactly what is collected.
- **`leads` is a non-tenant table** and must be registered as such with a reason — it is created before any client exists, so isolation suite A goes red unless it is on the exemption list.
- **Split honestly.** If you stop after step 6, the phase is *not* done: `plan/README.md` gets P29 `in-progress` plus a new P29a row, and the descoped steps 7-10 are written into `plan/v1/P29a-launch-hardening-and-drills.md` — not left in someone's head (SESSION-PROTOCOL C4).

## Session close
Run **`plan/SESSION-PROTOCOL.md` steps C1-C7**. Do not restate them here.

## Next-session prompt (paste this to start the next phase)
```
v1 phases P00-P29 are done (see plan/README.md). Do NOT start a new build phase.

Next session is the v1 release gate, then v2 planning:
1. Walk docs/LAUNCH-CHECKLIST.md row by row. Every row must name an artefact that exists in
   docs/evidence/ or be marked NOT DONE. Report the NOT DONE list verbatim — no summarising.
2. Confirm the gates in writing: Gate A (docs/evidence/P10-session-cost.md), Gate B
   (docs/evidence/P26-*.md), timed restore drill, timed key-ring restore drill, three security
   scans clean, full mandatory suite green (paste the verbatim scripts/ci.ps1 tail),
   check-copy green including Hindi. Gate C is NOT closed — no 10,000 claim may be published.
3. Resolve the founder's remaining open items in .memory/research/2026-08-26-v1r-scope-delta-and-decisions.md
   "Open questions" — especially 1 (real per-message prices, signup credit, low-balance threshold)
   and 6 (test numbers budget). The pricing page stays in "contact us" mode until 1 is answered.
4. Then plan v2 from plan/v2/README.md: run /feature for the first v2 phase only, write ADRs for
   anything that constrains future work, and cut plan/v2/ phase files just in time (next 3 only).

Use the agent roster in CLAUDE.md. Nothing is "done" without verbatim green test output.
```
