// Fixture: a clean tenant-facing component with no capacity figures at all.
export function CapacityFixtureClean(): string {
  return 'Sending is paused for this WhatsApp instance. Pending messages remain queued.';
}
