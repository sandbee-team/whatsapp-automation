# Fleet capacity — Gate B (P26)

Status: this fixture is the "no run duration" red-proof for
`check-capacity-gate.ts` - it carries the banner, a Measured N marker and an
error-bar marker but the run-duration marker has been stripped out entirely.

## Banner (verbatim — must appear unmodified in this file)

```
DRIFT VERDICT PENDING — P26a
```

## Measured N and run durations

- Measured N = `<FILL measured N>` synthetic instances (target 1,000)

95% CI `<FILL>`
