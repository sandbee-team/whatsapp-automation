# Session cost — capacity measurement (P10)

Status: SKELETON. This fixture is the "no banner" red-proof for
`check-capacity-gate.ts` - it carries a sample-size marker but the banner
line has been stripped out entirely.

## Sample size

N (sample size) = `<FILL AFTER U4 RUN>` resident sockets sampled per ramp
point.
