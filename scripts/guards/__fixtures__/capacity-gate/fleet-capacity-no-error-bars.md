# Fleet capacity — Gate B (P26)

Status: this fixture is the "no error bars" red-proof for
`check-capacity-gate.ts` - it carries the banner, a Measured N marker and a
run-duration marker but the error-bar marker has been stripped out entirely.

## Banner (verbatim — must appear unmodified in this file)

```
DRIFT VERDICT PENDING — P26a
```

## Measured N and run durations

- Measured N = `<FILL measured N>` synthetic instances (target 1,000)
- Run duration = `<FILL hh:mm>` (pacing run; 8 h target — see honesty note)
