# Fleet capacity — Gate B (P26)

Status: this fixture is the "no Measured N" red-proof for
`check-capacity-gate.ts` - it carries the banner, a run-duration marker and
an error-bar marker but the Measured N marker has been stripped out entirely.

## Banner (verbatim — must appear unmodified in this file)

```
DRIFT VERDICT PENDING — P26a
```

## Measured N and run durations

- Run duration = `<FILL hh:mm>` (pacing run; 8 h target — see honesty note)

95% CI `<FILL>`
