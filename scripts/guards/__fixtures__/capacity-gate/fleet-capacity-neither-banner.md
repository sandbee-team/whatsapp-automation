# Fleet capacity — Gate B (P26)

Status: this fixture is the "neither banner" red-proof for
`check-capacity-gate.ts` - it carries neither the pending banner nor a
closed-banner line, but does carry the other three markers.

## Measured N and run durations

- Measured N = `<FILL measured N>` synthetic instances (target 1,000)
- Run duration = `<FILL hh:mm>` (pacing run; 8 h target — see honesty note)

95% CI `<FILL>`
