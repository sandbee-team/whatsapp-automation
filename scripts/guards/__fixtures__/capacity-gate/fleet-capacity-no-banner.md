# Fleet capacity — Gate B (P26)

Status: this fixture is the "no banner" red-proof for
`check-capacity-gate.ts` - it carries a Measured N marker, a run-duration
marker and an error-bar marker but the banner section has been stripped out
entirely.

## Measured N and run durations

- Measured N = `<FILL measured N>` synthetic instances (target 1,000)
- Run duration = `<FILL hh:mm>` (pacing run; 8 h target — see honesty note)

95% CI `<FILL>`
