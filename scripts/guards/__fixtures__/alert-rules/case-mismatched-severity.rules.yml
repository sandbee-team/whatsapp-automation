groups:
  - name: fixture-case-mismatched-severity
    rules:
      - alert: CaseMismatchedSeverity
        expr: wp_instances_unowned > 0
        labels:
          severity: Page
        annotations:
          summary: 'fixture: severity case must match exactly'
          description: 'fixture only - never a real alert'
          runbook_url: docs/RUNBOOK.md#alert-instances-unowned
