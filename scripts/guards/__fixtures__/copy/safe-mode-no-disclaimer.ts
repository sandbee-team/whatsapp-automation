// Known-bad fixture for check-copy.test.ts (P00 step 9).
// Excluded from every real scan (scripts/guards/__fixtures__/**).

// Bad: mentions Safe Mode but never ships the disclaimer - must be flagged.
export const safeModeCardCopy = 'Turn on Safe Mode to pace your sending automatically.';
