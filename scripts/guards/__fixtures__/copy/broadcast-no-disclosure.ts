// Known-bad fixture for check-copy.test.ts (P00 step 9).
// Excluded from every real scan (scripts/guards/__fixtures__/**).

// Bad: mentions Broadcast but never ships the disclosure - must be flagged.
export const broadcastCardCopy = 'Start a new Broadcast to reach your contacts.';
