node_modules
dist
build
coverage
pnpm-lock.yaml

# Off-limits / not-yet-in-scope (see CLAUDE.md)
demo
.memory
.claude
plan
plan.html
MASTER-PLAN.md
CLAUDE.md
my-prompt.md

# Machine-written test snapshot (vitest --update writes it; prettier's array style conflicts)
db/schema/grants.snapshot.json

# TanStack Router codegen - regenerated on every dev/build, never hand-edited (P04b UB2)
app/frontend/src/routeTree.gen.ts

# @wp/design-tokens generated output (pnpm tokens:build) - never hand-edited (P05 step 1)
packages/design-tokens/css/tokens.css
packages/design-tokens/css/tailwind-theme.css
packages/design-tokens/src/tokens.generated.ts

# Machine-local object-store data (P20 Unit U3) - uploaded tenant CSVs
.data/

# Playwright output dir (written by every e2e run; machine-local tool output, never reviewed)
test-results

# Machine-written LCP gate evidence (regenerated by every `pnpm -F website run test:perf`; P29 step 6)
docs/evidence/P29-lcp-india4g.md
docs/measurements/2026-09-08-lcp-india4g.json

# Next.js build output / generated env (website, P29)
website/.next
website/out
website/next-env.d.ts

# Machine-written drill evidence (regenerated by every real drill run; P29a steps 8-9)
docs/evidence/P29-restore-drill.md
docs/measurements/2026-09-08-restore-drill.json
docs/measurements/2026-09-08-keyring-restore-drill.json

# TanStack Router codegen for the admin panel - regenerated on every dev/build, never hand-edited (same rule as app/frontend)
admin/frontend/src/routeTree.gen.ts

# Hand-authored deployment guide (2026-09-14). Prettier reflows HTML, and this
# file's <div class="cmd"> blocks are `white-space: pre` shell commands - the
# formatter collapses their newlines, which silently joins two separate
# commands into one broken line a reader would paste. Verified: `chmod 400 ...`
# and `ssh -i ...` became a single line. The content is prose and commands, not
# code, so there is nothing for the formatter to standardise anyway.
docs/runbooks/first-deploy-guide.html
