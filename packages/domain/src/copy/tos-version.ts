/**
 * The date printed at the top of `website/content/legal/terms.mdx` §12;
 * bump when the terms change. The onboarding consent step records this
 * value on the workspace (P29a step 10, migration 0075).
 */
export const TOS_VERSION = '2026-09-08';

export const TOS_VERSION_PATTERN = /^\d{4}-\d{2}-\d{2}$/;
