/**
 * @wp/testkit - tenant/instance/job factories, fake clock, in-memory
 * provider adapter double, Postgres container bootstrap. Never imported by
 * production code (lint-enforced); test suites only.
 */
export const packageName = '@wp/testkit' as const;
